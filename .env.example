# GEMINI_API_KEY: Required for Gemini AI API calls if needed in the future
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# PORT: The port the backend server or app will run on when served locally in production
PORT=3000
